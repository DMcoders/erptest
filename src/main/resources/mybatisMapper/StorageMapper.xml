<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.erp01.mapper.StorageMapper">

    <insert id="inStore" parameterType="java.util.List">
        INSERT INTO storage(storehouseQcode,tailorQcode) VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.storehouseQcode},#{item.tailorQcode})
        </foreach>
    </insert>

    <delete id="outStore" parameterType="java.util.List">
        DELETE FROM storage WHERE tailorQcode IN
        <foreach collection="list" item="item" open="(" separator="," close=")">
            #{item}
        </foreach>
    </delete>

    <select id="getStorageState" resultType="map">
        SELECT storehouseQcode,COUNT(*) as storageState  FROM storage GROUP BY storehouseQcode
    </select>

    <!--<update id="changeStore" parameterType="java.util.List">-->
        <!--<foreach collection="list" item="storage" separator=",">-->
            <!--UPDATE storage SET-->
            <!--storehouseQcode=#{storage.storehouseQcode}-->
            <!--WHERE tailorQcode=#{storage.tailorQcode}-->
        <!--</foreach>-->
    <!--</update>-->

</mapper>